{
  "name": "aws-sdk-go-v2-ifaces",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "",
  "scripts": {
    "clone": "./scripts/git_clone_aws-sdk.sh",
    "gen_ifaces": "./scripts/gen_ifaces.sh",
    "lint": "golangci-lint run --fix",
    "prepare": "npm run sync && go mod tidy -e && go install github.com/vektra/mockery/v2@v2.39.2 && go mod vendor && sort-package-json",
    "sync": "npm run clone && npm run gen_ifaces",
    "test": "npm run sync && npm run test:go",
    "test:go": "go test ./service/..."
  },
  "devDependencies": {
    "commit-and-tag-version": "12",
    "commitlint": "19",
    "sort-package-json": "2.10"
  }
}
